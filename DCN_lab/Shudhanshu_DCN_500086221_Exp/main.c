#include <stdio.h>
#include <stdlib.h>

int main()
{
    int K_max = 10, f;
    int randomnumber;
    randomnumber = rand() % 2;
    printf("Enter the number of Frames: %d", f);
    scanf("%d", &f);

    return 0;
}
